var ftpConfig = {
    host: "ftp host",
    port: 21, 
    user: "user name", 
    password: "password" 
  }

var serverConfig = {
	domaineName: "http://domain name.ext/",
	subFolder: "subfolder"
	
}

module.exports = ftpConfig;